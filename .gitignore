# The compiled static site is built into public/.

# Ignore everything at the repo root...
# /*

# ...except the compiled site and this ignore file.
# !/public/
# !/.gitignore

# Keep OS junk out of the published folder.
public/**/.DS_Store

node_modules/
cloudflare-worker/node_modules
